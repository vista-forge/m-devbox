# Inherit the org-root .envrc (~/vista-forge/.envrc), which loads the shared
# auth file ~/data/vista-forge/auth.env (engine/VistA creds via direnv) and the
# workspace env (GOPROXY=file://… for airgapped Go builds — stage-context.sh
# rebuilds `m`/`m-ydb` and needs it).
source_up
